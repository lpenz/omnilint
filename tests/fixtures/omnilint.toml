# Project configuration for the omnilint test fixtures.
#
# proselint is not packaged in nixpkgs, so it is not available in the
# development shell used to run the test suite: keep it disabled here so
# that the fixture-based tests are deterministic regardless of whether it
# is installed on the `PATH`.

[linters.proselint]
mode = "disabled"
